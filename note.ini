-- Ici on a decouvert comment créer des composants pour sa:
    On crée une fonction en utilisant de pascalCase{majuscule au début}
    Cette fonction peut avoir un ensemble de proprieté comme un booléan, null, indefined, des nombres de strings
    On ne peut pas faire de mutation dans le cadre du react
    On ne peut pas utiliser les hook dans les boucle et les conditions
    Pas de retun avant hook